<header id="header" role="banner">
    <div class="quicksearch">
        <form action="{{ path('quick_search') }}" method="get" name="quick_search" id="quick_search">
            <input type="text" name="q" id="q" value="" placeholder="{{ 'gyorskereső'|trans() }}">
            <button type="submit" class="searchbutton"><span class="searchicon"></span></button>
        </form>
    </div>
    <div class="headright">
        {% if app.user %}
            {{ 'Bejelentkezve:'|trans() }} <span class="username">{{ app.user.username }}</span>
        {% endif %}
    </div>
    <div class="clear"></div>
</header>